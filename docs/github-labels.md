# GitHub Label Set

Apply labels from `.github/labels.json` after repository creation.

Recommended automation command (requires `gh` CLI with repo admin permission):

`gh label delete --yes <label>` and `gh label create ...` or use a label sync action.

Status labels map directly to FIP lifecycle stages.
