# finas

`finas` is an optional Fin-native assembler track.

Current default architecture prefers direct machine-code encoding from compiler IR.
`finas` exists only if maintaining a text assembly interface becomes necessary.
