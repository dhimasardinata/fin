# Examples

Examples are runnable Stage0 sources used as documentation and test inputs.

- `stage0/source_module_layout/main.fn`: source/module layout example for `FIP-0004`.

Run example checks with:

```powershell
./tests/integration/verify_examples.ps1
```
